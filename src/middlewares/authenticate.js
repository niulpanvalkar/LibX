import jwt from "jsonwebtoken";

class Authenticate {
    constructor () {}

    authenticate = async (req, res, next) => {
        // take the username and password from the request and compare it with the one in DB 
        
    } 
}